- name: "configure {{ component }} server"
  hosts: "{{ component }}"
  become: yes
  roles:
  - "{{ component }}"

  # - name: Configure components
  #   hosts: "{{ item }}"
  #   become: yes
  #   vars:
  #     components:
  #       - db
  #       - backend
  #       - frontend
  #   roles:
  #     - "{{ item }}"
  #   loop: "{{ components }}"
